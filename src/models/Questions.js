import React from "react";

export class Quesions{
    constructor(story,question,category,options) {
      this.story = story;
      this.question = question;
      this.category=category
      this.options = options;
    }
  }